## Packages
(none needed)

## Notes
FiraGO font is loaded via @import in client/src/index.css (fallback to system sans if unavailable).
Only route is /admin-add; / redirects to /admin-add.
Image Upload is implemented as an Image URL text input to match schema (imageUrl).
